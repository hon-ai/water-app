# M5 Handoff — Heatmap Audiovisualizer

> **For the agent picking this up:** read this file end-to-end before doing anything else. It is the compressed continuation of M1 → M1.5.1 → M2 → M2.5 → M3 → M4. A fresh session armed with this doc should be productive within five minutes.

**Status at handoff:** M4 work complete (T1–T33 shipped). T34's automated audit is green; the **manual smoke walk against `eval/m4_acceptance/`** still needs to be done before tagging `m4`. See § 5 for the smoke checklist.

---

## 1. The one-paragraph product context

Water is a Tauri 2 desktop writing app with one heretical UX rule: **the writer never types into a chat box and the LLM never sees a chat box.** The LLM sees only the manuscript text plus the click history of small "pills" that surface in the margin while the writer works. Pills come from named "speakers" — generic personas (Echo / Architect / Editor / Cartographer / Chorus) or character voices drawn from LSM v2.1 sheets — and their language is reactive/observational, never instructive. The writer can expand a pill into a bouquet of three child pills, expand any child into another bouquet, regenerate a bouquet, pin a pill to a quiet column at the right margin, or dismiss the whole thread. Local-first with cloud opt-in.

**M4 shipped the World/Setting Bible.** M5 will ship the Heatmap audiovisualizer that gives the writer a per-scene visualization of pacing, valence, coherence, character presence, and now world-entry references.

---

## 2. Repo state you are inheriting

### Tags

| Tag | SHA | What's in it |
|---|---|---|
| `m1-foundation` | (early) | Rust core, Python sidecar, Tauri shell, 16-table SQLite schema, snapshot system, LLM router with 5 adapters |
| `m1.1` | `b1716de` | 14-task correctness patch |
| `m1.5` | `78b0f6e` | 13-task UI chrome redesign |
| `m1.5.1` | (M1.5 follow-up) | Carry-over fixes |
| `m2` | (M2 closure) | 30-task pill engine |
| `m2.5` | `d13f484` | Rich-text marks (bold/italic/link) + selection toolbar |
| `m3` | `846f9a8` | LSM v2.1 character system — 22 tasks |
| `m4` | **PENDING manual smoke** | World/Setting Bible — 33 tasks shipped, 1 (manual smoke + tag) pending |

### Branch / workflow

- Default branch is `master`. All milestones tagged directly on `master`.
- M4 commit map: `git log --oneline m3..HEAD` shows 33 feature/test commits between `m3` and the pending `m4` tag. M3 ran 22 + many doc commits; M4 ran 33 + the fixture commit + this handoff.

### Tree state at M4 close

- `app/` — Tauri 2 shell + React 18 + TS strict (`noUncheckedIndexedAccess: true`).
- `crates/water-core/` — Rust 2024 engine. Migrations at **v4** post-M4 (added `world_segment.template_json/hidden/hue_token/slug/created_at/updated_at`, `world_entry.aliases_json/schema_version/created_at/updated_at`, `pinned_pill.origin_trigger`, and the `world_entry_by_segment` index).
- `app/src/intake/` — `ConversationalIntake`, schema-agnostic. Drives both character intake (M3) and world entry intake (M4 T24).
- `app/src/characters/` — M3 surface (CharacterIndex / CharacterCard / CharacterSheet / InlineField).
- `app/src/worlds/` — M4 surface (WorldsSurface / WorldIndex / WorldSegmentTile / WorldSegmentView / WorldEntryCard / WorldEntrySheet / WorldEntryIntakeSheet / SegmentTemplateEditor / WorldSettingsMenu).
- `app/src/util/flattenSerdeFlatten.ts` — generic helper extracted from M3's `flattenCharacterData.ts`; consumed by both character and world surfaces (M4 T19).
- `app/src-tauri/src/commands/world.rs` — all M4 IPC. Mirrors `character.rs`'s `_core` async-fn pattern.
- `crates/water-core/src/world/` — `templates.rs`, `store.rs` (single-doc + collection), `registry.rs` (case-insensitive name+alias index), tests.
- `prompts/speakers/persona/cartographer.toml` — replaced the M2 placeholder with the world-aware Cartographer template (M4 T18, tone-audit-clean).
- `prompts/speakers/character/template.toml` — gained the Setting block (M4 T26, with line-omission for None location).
- `prompts/tasks/world_drift_check.toml` — Stage 2 confirmation prompt (M4 T17).
- `eval/m4_acceptance/` — Pell Library fixture + 4 test scenes + README (M4 T33).

### Test counts at M4 close (vs. m3)

| Layer | At M4 close | At `m3` | Δ |
|---|---:|---:|---:|
| `cargo test -p water-core` (lib + integration) | 283 | 201 | +82 |
| `cargo test -p water-app` | 31 | 19 | +12 |
| `pnpm @water/app test` (TS) | 199 | 157 | +42 |
| **Total** | **~513** | **377** | **+136** |

Everything green at the time of writing. Run all three commands as your first step in a new session to confirm nothing has drifted.

---

## 3. The M5 scope, in your hands

The locked v1 design spec is `docs/superpowers/specs/2026-05-16-water-design.md`. **Read § 4.7 (M5 plan) + § 3.7 (Heatmap schema) + § 6.4 (heat metrics) before writing the M5 spec.** No separate M5 spec doc yet — write one as your first phase (same pattern that produced M3 and M4 specs).

**M5 ships:**

1. **Heatmap visualization surface.** Per-scene strip rendering pacing, valence, coherence, character-presence intensity, and world-entry references. Reactively updates as the writer types.
2. **Heat metric computation.** Per-paragraph rolling scores; cached in SQLite, recomputed on body change with O(Δ) cost.
3. **Heatmap-aware pill triggers.** A few existing triggers (pace_floor, scene_flow_dip) gain heatmap awareness — they fire on heatmap-derived signals rather than ad-hoc heuristics.
4. **Optional: embedding upgrade path.** Spec § 4.7 mentions M5 as the natural place to land embedding-based versions of the M2/M3 cheap heuristics (anti-loop Jaccard, lemma overlap, contextual-overlap). Whether this lands in M5 or M6 depends on scope.

**Exit criteria** (verbatim from spec § 4.7):

- Heatmap strip renders all five metrics for the active scene.
- Writer can drag the heatmap to navigate the scene.
- Heatmap recomputes within 1 s of an autosave landing on a 5 000-word scene.
- At least one pill trigger has been migrated to a heatmap-derived signal.

**Explicitly NOT in M5:**

- Macro spatial scene canvas (M6).
- Customization, polish, tester build (M7).
- Multi-scene heatmap aggregation (M6 candidate).

---

## 4. The absolutely-do-not-violate constraints

All M2/M3/M4 hard constraints still apply. M5-specific additions will surface once you write the M5 spec; until then, inherit the M4 set:

1. **The LLM NEVER receives conversational input from the user.**
2. **Reactive / observational pill tone.** Persona blacklist + 3-layer enforcement (prompt clause, PASS, post-hoc regex).
3. **Local-first.** No new cloud deps.
4. **Determinism.** `(trigger, scene_state, project_state, persona_state, character_state, world_state)` → same prompt assembly + speaker selection.
5. **Human-readable on disk.** `world/<segment>/<entry-ulid>.toml` for collections; `world/<segment>.toml` for single-doc. SQLite is rebuildable from disk per § 3.8.
6. **Plugin-contract design.** Whatever M5 adds should remain `Speaker`-compatible / `Trigger`-compatible.
7. **Pastel-glow visual identity + Notion/Apple-fluid motion.**
8. **`ConversationalIntake` MUST stay schema-agnostic.**
9. **Lock ordering invariant** (KNOWN_FRAGILE #6): `project lock → drop → write-lock → db lock`.

---

## 5. Outstanding work before `m4` tag

**The automated audit is green.** What remains:

### 5a. Manual smoke walk (REQUIRED before tagging)

Walk `eval/m4_acceptance/README.md`:

```powershell
$env:Path = "$env:USERPROFILE\.cargo\bin;$env:Path"
pnpm --filter @water/app tauri dev
```

Follow the 6-step usage list in the README:
1. Fresh project.
2. Open World → Locations → + New entry → walk intake with the Pell Library values.
3. Open the entry sheet, add aliases.
4. Create scene; set location to Pell.
5. Paste each of the 4 test scenes in turn.
6. Observe pill margin per the expected-behaviour table.

Record pass/fail per scene. Any failure becomes either a hotfix or a known limitation appended to `KNOWN_FRAGILE.md`.

**Also walk the M3 § 18 manual smoke that was un-walked at the `m3` tag** if you haven't already during M4 (it has not been done as part of the M4 work). Use `eval/m3_acceptance/marcus_vale.toml`.

### 5b. Tag `m4`

```powershell
git tag -a m4 -m "M4 World/Setting Bible — 33 tasks shipped + Pell Library acceptance fixture"
```

Do NOT tag until 5a passes. If a smoke item fails: hotfix, re-run automated audit, then tag.

### 5c. (Optional) M3-polish batch

The M3 carry-over polish backlog (17 items) is documented in `docs/superpowers/handoffs/2026-05-19-m4-handoff.md` § 6. None of these were addressed during M4 — they remain non-blocking. The M3 polish items most worth clearing before M5:

- **T12** — `update_field` non-string `main.full_name` silent data loss. 3-line guard.
- **T2** — round-robin claim test seeds only 4 characters (also worth replicating for `world_segment` round-robin).
- **T3** — silent JSON-parse swallowing in `character/registry.rs:60` (and `world/registry.rs` mirrors the pattern).
- **T7** — `tokenize(paragraph)` recomputed per (field × character). Same pattern in `world_drift::contextual_overlap` — hoist the tokenization out of the entry loop.

Treat these as opportunistic clears; bundle into a `m4-polish` PR after tagging.

---

## 6. M4 polish carry-forward (new debt from this milestone)

These are issues I noticed during implementation but did not address. None are blocking. Roll into a polish PR or opportunistically during M5 work when M5 work touches the same module.

1. **`SceneContext::empty` in `voice/mod.rs`** generates a fresh `Id::new()` per call. Cheap (ULID), but it's a small amount of nondeterminism that shows up in registry-build paths. M5 polish: have `empty()` return a static `Id` (or `Id::nil()` if such a thing is added).
2. **`pill_pin_core` builds a `WorldStore` inside the closure** that holds the DB lock. The DB lock is `Mutex<Db>`; the store borrows `&Db`. This is correct but somewhat tight — if the seed write ever stalls, the pin path blocks. Consider a separate write-lock for the stub-creation branch.
3. **`Bouquet.tsx` dispatches `water:nav-world-entry` via `window`** — works, but couples Bouquet to the worlds-surface routing primitive via a string event name. M5 may want a typed event bus.
4. **`SegmentTemplateEditor` doesn't normalize repeated whitespace in derived field ids.** "System  Name" (two spaces) becomes `main.system_name` after the `[^a-z0-9]+` collapse; "System Name " (trailing space) likewise. Fine for v1; future-proof by surfacing the derived id under the input.
5. **`WorldSettingsMenu` confirms via `window.confirm`** — synchronous, blocks the UI thread, and is jarring against the pastel-glow chrome. Spec § 4.7 doesn't require a custom modal, but it's worth replacing with a `<Sheet>`-styled confirm in M7 polish.
6. **The `pinned_pill.origin_trigger` column duplicates `trigger_class`.** Both store the same value today (`pill.trigger_id`). The split exists so M5+'s Heatmap can attach richer classification metadata without disturbing the origin-trigger lookup used by the Chorus-stub branch. If M5 doesn't actually need the split, drop `origin_trigger` in a v5 migration.

---

## 7. Hard-earned lessons from M4 (carry these forward)

### Process

- **TDD discipline held for ~70% of M4 tasks.** Tasks 25, 31, 32 followed strict RED→GREEN; task 29 wrote test + implementation together (still verified the implementation against behavioral assertions, but the "watch it fail first" step was skipped). When the test scaffolding requires new types or modules that don't exist yet, the RED step degenerates into a compile-error — not a behavioral failure. Pre-build the type signature with a `todo!()` body to get a meaningful RED.
- **Migrations + schema mirror tests are cheap insurance.** The v4 migration tests caught two typos before any other code ran. Replicate the M3 T2 / M4 T1 pattern for M5's heatmap tables.
- **`_core` async-fn extraction paid off again.** Every M4 Tauri command has a testable `*_core`. The pill-pin tests (T29) drove the stub-creation branch via the core fn alone; no Tauri harness required. Bake this pattern into M5 from the start.
- **Mocking only the IPCs you exercise leaves landmines.** WorldsSurface.test.tsx's stub-routing test failed once because the rendered WorldEntrySheet called an IPC the test hadn't mocked. The fix was to mock the full set the downstream component touches. M5: when adding a new route, list every IPC the destination component reads on mount.

### TypeScript / Vitest

- **`pillPin` returning `Promise<void>` vs. `Promise<PinPillResponse>`** is a silent type-narrowing migration. Existing `void ipc.pillPin(...)` call sites compile under both shapes (the `void` operator discards). Tests that mocked `pillPin: vi.fn().mockResolvedValue(undefined)` continue to pass at runtime but are wrong at the type level — `.mockResolvedValue` in `vi.fn()` is typed as `any`, so the mismatch never surfaces. M5: when widening a return type, grep mock factories and update them too.
- **`findByRole("button", { name: /Foo/ })`** is ambiguous when both a primary button and a `Dismiss Foo` button are in scope. The fix is a precise regex anchor (`/^Foo$/`) or `findByRole("button", { name: "Foo" })` (exact match). T28 ate ~20 minutes on this.
- **`findByText` is also ambiguous when an `<option>` and a `<button>` share the same text content.** Use a more specific selector (role + name) to disambiguate.
- **`findByText`/`findByLabelText` race against React effects.** When the assertion races a `useEffect` that fires an async IPC, prefer `findByText` (which waits) over `getByText` (which doesn't).

### Rust

- **`Db: Send + !Sync`** still bites in async paths. M4 introduced no new lock-ordering surprises, but the pattern `lock → drop → lock again` reappears in `pill_pin_core` (two separate `db.lock().await` calls bracketing the stub-create branch). The plan was to do this in one critical section, but the WorldStore needs `&Db` so the lock has to land once, and the test fixture wants to inspect the post-write state, which requires a fresh lock. Working as intended; flagged for awareness.
- **`rusqlite::Statement: !Send`** rules still apply. Drain queries to owned `Vec<T>` inside a sync block before yielding.

### Windows / PowerShell

- **`[System.IO.File]::WriteAllText(...)` for clean UTF-8** is still the safest path. PowerShell 5.1's `Set-Content -Encoding UTF8` writes a BOM, which `toml::from_str` accepts but `serde_json::from_str` does not.
- **CRLF/LF warnings on every commit** are environmental. Ignore.
- **`NativeCommandError` on cargo stderr** is cosmetic; the exit code is what matters.

---

## 8. Critical files and where to find things

### Specs / plans / handoffs

- `docs/superpowers/specs/2026-05-16-water-design.md` — the locked v1 design spec.
- `docs/superpowers/specs/2026-05-19-m4-world-bible-design.md` — M4 spec (closed).
- `docs/superpowers/plans/2026-05-19-m4-world-bible.md` — M4 plan (executed, 33 tasks shipped).
- `docs/superpowers/handoffs/2026-05-19-m4-handoff.md` — the M4-incoming handoff (now historical).
- `docs/superpowers/handoffs/2026-05-20-m5-handoff.md` — **this file**.

### Documentation

- `KNOWN_FRAGILE.md` — **22 entries** at M4 close. M4 added #18 (collision resolution heuristic), #19 (contextual-overlap pre-check), #20 (Cartographer hue), #21 (unnamed stub persistence), #22 (case-sensitivity asymmetry).
- `eval/m4_acceptance/README.md` — fixture for `world_drift` regression smoke.
- `eval/m4_acceptance/pell_library.toml` — reference location entry.
- `eval/m4_acceptance/scenes/{consistent,contradiction_sunlight,contradiction_elevation,near_miss_unrelated}.md` — 4 test scenes.

### Code entry points (engine, post-M4)

- `crates/water-core/src/lib.rs` — exports.
- `crates/water-core/sql/v1_init.sql` + `v2_pill_engine.sql` + `v3_character_hue.sql` + `v4_world_bible.sql` — schema.
- `crates/water-core/src/world/` — full M4 surface (`templates.rs`, `store.rs`, `registry.rs`).
- `crates/water-core/src/world/registry.rs::WorldRegistry` — case-insensitive name+alias index.
- `crates/water-core/src/orchestrator/triggers/world_drift.rs` — Stage 1 name+alias + contextual-overlap, with cooldown deferred to M5 polish.
- `crates/water-core/src/orchestrator/collision.rs` — `resolve_token_kind` (character-in-scene wins).
- `crates/water-core/src/prompts/tasks/world_drift_check.toml` — Stage 2 confirmation prompt template.
- `crates/water-core/src/voice/router.rs::WORLD_TRACK_TRIGGERS` — Cartographer voice routing.
- `crates/water-core/src/voice/speaker.rs::CartographerSpeaker` — world-aware persona speaker.
- `crates/water-core/src/voice/character_template.rs` — `{{world.location_*}}` token injection (M4 T26).
- `crates/water-core/src/voice/mod.rs::SceneContext` — per-dispatch scene context for character-voice rendering.

### Code entry points (commands, post-M4)

- `app/src-tauri/src/commands/world.rs` — full segment + entry CRUD + autosuggest.
- `app/src-tauri/src/commands/pill.rs::pill_pin_core` — origin_trigger plumbing + Chorus stub creation.
- `app/src-tauri/src/state.rs::WorldWriteLocks` — per-entry write-lock registry.

### Code entry points (UI, post-M4)

- `app/src/worlds/WorldsSurface.tsx` — five-view router (index / segment / entry / entry-intake / new-segment) + `water:nav-world-entry` event listener.
- `app/src/worlds/SegmentTemplateEditor.tsx` — create + edit with append-only built-ins.
- `app/src/worlds/WorldSettingsMenu.tsx` — visibility toggle + delete user-added.
- `app/src/scenes/SceneMetadataSheet.tsx` — gained the Location selector (M4 T27).
- `app/src/scenes/sceneMetadataChannel.ts` — `ChipSuggestion` discriminated union (character + world variants).
- `app/src/scenes/SceneAutosuggestChips.tsx` — renders both chip kinds.
- `app/src/chrome/EditorCanvas.tsx` — autosuggest fan-out runs character + world scanners in parallel.
- `app/src/pill/Bouquet.tsx` — pin handler dispatches nav event on stub creation.
- `app/src/util/flattenSerdeFlatten.ts` — generic `#[serde(flatten)]` flattener (M4 T19, reused by both surfaces).

---

## 9. Quick-resume block for a fresh session

> I'm continuing work on **Water** — a Tauri 2 desktop writing app with a non-generating LLM pill engine. The previous session completed M4 (World/Setting Bible) work; the `m4` tag is pending the manual smoke walk against `eval/m4_acceptance/`. Repo is at `C:\Users\H BLAUNTE\Water`. Read `docs/superpowers/handoffs/2026-05-20-m5-handoff.md` end-to-end before doing anything else. After reading: (a) walk the manual smoke; (b) tag `m4` if clean; (c) brainstorm M5 (Heatmap audiovisualizer) using `superpowers:brainstorming` — start with metric definitions and the per-paragraph cache shape.

---

## 10. Open questions to bring into the M5 brainstorm

1. **Per-paragraph or per-block heatmap cache?** Spec § 3.7 sketches a `heat_metric` table; the granularity (paragraph vs. block) drives storage + recompute cost.
2. **Which existing triggers migrate first?** `pace_floor` is the obvious candidate (already pacing-heuristic-based). `scene_flow_dip` is the second.
3. **Embedding upgrade scope — M5 or M6?** Replacing the M2/M3 cheap heuristics (anti-loop Jaccard, lemma overlap, contextual-overlap) with embeddings is a natural M5 fit but expands M5 scope by ~10 tasks. Default: defer to M6 unless the writer's M4 smoke turns up frequent over-fires.
4. **Heatmap drag-to-navigate UX.** Coarse drag (scene jump) vs. fine drag (paragraph jump)? Both?
5. **Hue palette for the 5 metrics.** Reuse `--water-hue-pace`, `--water-hue-valence-pos`/`neg`, `--water-hue-coherence`, `--water-hue-character-default`, and a new `--water-hue-world-default`? Or commission a new set scaled to the strip's small surface?
6. **Heatmap rerender cadence.** Same 2 s autosave debounce, or a separate scroll-aware cadence?

---

When you (the next agent) finish reading this: respond with a one-paragraph summary of M5's scope back to the user to confirm calibration, then ask whether to walk the un-completed M4 § 13 manual smoke first OR jump straight into the M5 brainstorm.
